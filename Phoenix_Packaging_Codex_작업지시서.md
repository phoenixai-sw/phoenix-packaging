# 피닉스AI 포장디자인 플랫폼 구축 작업지시서

작성일 2026년 9월 16일 · 발주자 주식회사 피닉스에이아이 · 구현 담당 Codex

이 문서는 국내 식품 브랜드가 포장 시안을 만들고, 한글 문구를 직접 수정하며, 정확한 규격의 칼선과 바코드를 배치해 인쇄 의뢰 파일을 준비하는 웹 플랫폼의 개발 명세다. Codex는 설명이나 화면 시안에 그치지 말고 저장·재편집·파일 출력·크레딧 정산까지 연결된 애플리케이션을 구현한다. 개발용 이름은 Phoenix Packaging, 저장소명은 phoenix-packaging으로 제안한다.

기준 문서는 「AI 패키지 제작 자동화와 ERP 품절 처리 사업 전략」 최종판이다. 이 작업은 포장디자인 사업을 구현한다. ERP 품절 처리, Aside 연동, 자체 업무 브라우저, 쇼핑몰 통합관리는 별도 사업 범위다. Codex는 개발과 검증에 사용하며, 고객의 이미지 생성은 서버의 정식 API 연결로 제공한다.

## 1 착수 지시와 완료의 의미

먼저 현재 저장소, 적용되는 AGENTS.md, 실행 환경과 기존 코드를 확인한다. 기존 구현이 있으면 활용하고 사용자 변경을 보존한다. 새 저장소라면 아래 기본 구조로 시작한다. 구현에 필요한 일반 선택은 이 문서의 기본값으로 진행하고, 외부 계정이나 제조사 자료가 없으면 해당 연결을 개발 모드로 분리한 상태에서 나머지 작업을 완성한다.

한 번에 모든 기능을 얕게 만드는 대신 첫 번째 세로 흐름을 완성한다. 사용자가 가입하고, 3면 실링 봉투를 선택하고, 상품 정보를 입력하고, 생성된 배경 위에 한글을 편집하고, 저장한 프로젝트를 다시 열어 검토용 PDF를 받는 흐름이다. 이후 과금, 스탠드형, 제작 파일 검증, 팀 작업, 박스 순서로 확장한다.

완료 상태는 세 가지로 구분한다.

| 상태 | 반드시 갖춰야 하는 결과 | 완료로 표현할 수 있는 범위 |
|---|---|---|
| 개발 데모 완료 | 실제 DB 저장, 편집·복구, 샘플 출력, 모의 AI·결제와 정산 테스트 | 로컬 개발 데모 |
| 외부 연동 검증 완료 | 실제 이미지 API와 PG 테스트 환경, 원가 기록, 실패 복구 | 실제 API 연결을 검증한 시험판 |
| 유료 시험 운영 가능 | 제조사 승인 조합, 실물·출력 검수, 운영 결제·정책·백업 | 승인 범위의 유료 시험 서비스 |

외부 연결을 모의 처리한 화면이나 제조사 미확인 도면을 상용 완료로 보고하지 않는다. API 키가 없다는 이유만으로 개발 전체를 중단하지 않는다. 마지막 보고에는 실행 방법, 구현 항목, 통과한 검수, 남은 외부 연결을 구분한다.

## 2 사업 요구사항과 구현 원칙

확정된 사업 방향은 월 구독에 따라 크레딧을 지급하고 실제 작업에 사용하는 방식이다. 요금, 크레딧 수량, 좌석 수, 유효기간은 최종 보고서의 제안값을 초기 설정으로 사용한다. 운영자가 정책 버전으로 바꿀 수 있게 하며 코드 여러 곳에 숫자를 중복 입력하지 않는다.

| 요구사항 | 구현 지시 | 검수 연결 |
|---|---|---|
| 한국어 문구 직접 수정 | 배경과 독립된 텍스트 객체, IME 입력, 글꼴과 줄바꿈 보존 | AC02 AC03 |
| 실제 규격과 칼선 | mm 기반 구조 엔진과 버전이 있는 제조사 템플릿 | AC05 AC06 |
| 바코드 생성과 배치 | 고객 번호 검증, 벡터 생성, 여백·가공영역 검사 | AC07 |
| 원형 걸이 구멍 | 지름과 위치 입력, 제조사 허용영역 검사 | AC08 |
| 전체 면 확인 | 2D 면 목록과 3D 매핑을 같은 face_id로 연결 | AC09 AC10 |
| 편집 가능한 결과 | 원본 프로젝트와 자산 저장, 불변 리비전, 재열기 | AC04 |
| 인쇄 의뢰 자료 | 실제 크기 PDF, 미리보기, 작업 정보, 검수 결과 | AC11 AC12 |
| 구독과 크레딧 | 팀 공용 원장, 예약 후 성공 차감, 중복 방지 | AC14부터 AC22 |
| 쉬운 작업 흐름 | 초보자 단계형 안내, 전문가용 세부 옵션 | AC01 |
| 경쟁력 검증 | 작업 시간, 직접 수정 성공, 입고 결과, 원가 기록 | AC30 |

이미지 모델에 한글 문구·상품 번호·칼선을 그리게 하고 그 결과를 제작 데이터로 사용하지 않는다. AI는 배경·일러스트·분위기와 배치 제안을 담당한다. 정확한 글자, 로고 원본, 치수, 바코드, 구조선은 별도 데이터와 결정적 연산으로 다룬다. AI가 만든 결과에 읽을 수 있는 임의 문구가 섞이면 경고하고 다시 생성하거나 제거한다.

Packify, Pacdora, Canva, Photoshop, Illustrator와 비교할 제품 기준은 기능 수가 아니라 동일 상품의 한글 수정, 여러 면 확인, 파일 준비에 필요한 시간과 재작업 수다. 경쟁사의 화면이나 자산을 복제하지 않는다. 포장 전용 업무 흐름과 자체 편집 데이터 모델을 만든다.

## 3 개발 범위와 출시 순서

### 3 1 첫 유료 시험에 필요한 범위

- 3면 실링 봉투와 스탠드형 봉투의 승인 가능한 템플릿 구조.
- 브랜드·상품 정보, 로고·제품 사진 업로드, AI 배경 생성과 수정.
- 한글 텍스트 편집, 이미지 이동·자르기, 레이어, 실행 취소, 자동 저장.
- 앞면·뒷면·바닥 등 템플릿에 실제 존재하는 모든 인쇄면 확인.
- mm 치수, 칼선·실링·접힘·인쇄 금지영역 표시, 원형 걸이 구멍.
- EAN-13 우선 지원, 고객 소유 번호의 검증과 벡터 배치.
- 검토용 출력, 승인 조건을 만족한 제작 파일 묶음, 재출력.
- 구독·추가 충전·크레딧 원장·팀 권한·관리자 템플릿 승인.
- 랜딩페이지, 체험 신청, 핵심 전환 측정, 도움말과 오류 안내.

### 3 2 후속 단계

박스는 승인된 구조 1종을 선택해 6면과 접착부·덮개·접힘선을 지원한다. 일반 직육면체의 여섯 면 그림만으로 실제 박스 도면을 대신하지 않는다. 상품별 일괄 변형은 먼저 프로젝트 복제로 제공하고 CSV 대량 생성은 후속으로 둔다. 원형 이외 걸이 구멍, DXF, QR·Code 128, 복잡한 특수포장과 실시간 동시 편집은 별도 확장이다.

AI·PSD 원본 저장, 모든 인쇄소의 자동 주문, 법정 표시사항 자동 인증, 아무 사진이나 완전 편집 가능한 레이어로 변환하는 기능은 초기 지원을 약속하지 않는다. AI 확장자가 필요하면 정식 호환 경로를 별도로 검증한다.

## 4 기본 기술 구조

다음은 신규 구현의 기본안이다. 기존 저장소에서 동등한 기능을 안정적으로 제공하면 불필요하게 교체하지 말고 변경 이유를 docs/decisions.md에 기록한다. 설치 시점에 지원되는 안정 버전과 라이선스를 확인하고 lockfile에 정확한 버전을 고정한다.

| 영역 | 기본 기술 | 책임 |
|---|---|---|
| 웹 | Next.js App Router와 TypeScript | 한국어 UI, 프로젝트 탐색, 결제 화면 |
| 편집기 | React Konva와 HTML 입력 요소 | 2D 편집과 한글 입력 |
| 3D | Three.js와 React Three Fiber | 면별 텍스처 미리보기 |
| API | Python FastAPI와 Pydantic | 권한, 프로젝트, 규격, 과금 규칙 |
| 데이터 | PostgreSQL와 SQLAlchemy 및 Alembic | 트랜잭션, 이력, 마이그레이션 |
| 작업 처리 | Celery와 Redis | AI·출력·정기결제 작업의 비동기 실행 |
| 파일 | S3 호환 객체 저장소 | 원본, 폰트, 결과물, 짧은 다운로드 URL |
| 로컬 파일 저장 | 저장소 어댑터의 디렉터리 모드 | 외부 계정 없는 개발 |
| 출력 | SVG 구조 데이터와 Python PDF 어댑터 | 실제 크기 벡터 출력, 검수 |
| 결제 | Toss Payments 어댑터와 모의 어댑터 | 국내 월 정기결제와 추가 충전 |
| AI | OpenAI 이미지 API 어댑터와 모의 어댑터 | 생성·편집, 결과 저장, 사용량 |
| 인증 | 서버 세션과 검증된 인증 라이브러리 | 비밀번호 해시, 세션, 이메일 인증 |
| 검증 | pytest, Vitest, Playwright | 금액·치수·IME·권한·E2E |

Next.js는 화면과 동일 출처의 API 라우팅을 제공하고, 업무 규칙은 FastAPI에 모은다. 프론트에서 계산한 잔액·금액·승인 상태를 신뢰하지 않는다. 웹 요청 프로세스에서 오래 걸리는 이미지 생성·PDF 처리·정기결제를 직접 실행하지 않는다. Redis는 큐·캐시에만 사용하고 크레딧의 최종 원장은 PostgreSQL에 둔다.

서버 세션은 무작위 토큰을 발급하고 DB에는 토큰 해시를 저장한다. 비밀번호는 검증된 Argon2id 구현을 사용한다. 쿠키는 HttpOnly·Secure·SameSite를 설정하고 변경 요청은 CSRF 방어를 적용한다. 로컬 개발에서는 메일을 로컬 수신함으로 보내고, 실운영에서는 메일 제공자를 설정한다. 가입, 인증, 로그인, 로그아웃, 비밀번호 재설정과 로그인 제한을 포함한다. 자체 암호 알고리즘을 만들지 않는다.

### 4 1 저장소 구성

| 경로 | 포함 내용 |
|---|---|
| apps/web | 랜딩, 계정, 편집기, 요금·관리자 화면 |
| services/api | FastAPI 앱, 인증, 라우트, 업무 서비스 |
| services/worker | Celery 실행, 스케줄러, outbox 소비 |
| packages/contracts | JSON Schema와 생성된 TypeScript 타입 |
| packages/editor | 장면 렌더링, 조작, 한글 입력, 이력 |
| packages/preview3d | 면별 UV 매핑과 템플릿별 3D |
| services/api/geometry | 칼선 계산, 좌표 변환, 충돌 검사 |
| services/api/exporters | 검토용·제작용 어댑터와 capability 검사 |
| config | 요금·차감표·기능 플래그 기본값 |
| fixtures | 자체 제작한 시험 자산과 미승인 템플릿 |
| tests | 정산, 규격, 권한, E2E와 출력 비교 |
| docs | 실행 안내, 결정 기록, 검수 결과, 운영 절차 |

개발자는 make dev로 웹·API·worker·PostgreSQL·Redis·로컬 메일을 시작하고, make seed로 시험 데이터를 넣으며, make test로 핵심 검수를 재현할 수 있게 한다. 이 명령은 Codex가 실제 Makefile과 Docker Compose로 구현할 계약이며 현재 제공된 애플리케이션 명령이 아니다.

## 5 화면과 사용자 경험

기본 언어는 한국어, 기본 단위는 mm, 금액은 원화다. 편집기는 PC 사용을 우선하고 1280px 이상에서 세 영역으로 구성한다. 모바일은 프로젝트 열람·업로드·결과 확인을 우선 지원한다. 편집 화면에는 기술 이름이나 내부 처리 용어를 드러내지 않는다.

| 화면과 경로 | 사용자에게 보여 줄 기능 |
|---|---|
| / | 대상 고객, 봉투 2종, 작업 예시, 요금·크레딧, 체험 시작 |
| /pricing | VAT 포함 결제액, 지급량, 만료, 차감 예시, 추가 충전 |
| /auth | 가입, 로그인, 이메일 확인, 비밀번호 재설정 |
| /app | 최근 프로젝트, 작업 상태, 저장·출력 이력, 잔액 |
| /app/brands | 브랜드 색상, 허용 폰트, 로고, 제품 자산 |
| /app/products | 상품, 맛·용량 변형, 바코드, 고객 확정 문구 |
| /app/projects/new | 포장·규격과 상품을 선택하는 단계형 생성 |
| /app/projects/{id}/editor | 면 목록, 캔버스, 속성, 3D, 저장·출력 |
| /app/projects/{id}/exports | 버전별 파일, 검수 결과, 제조사 확인 상태 |
| /app/billing | 요금제, 갱신일, 잔액 종류, 결제·차감·복원 이력 |
| /app/team | 좌석, 초대, 역할, 고객사 작업 공간 |
| /admin | 템플릿·제조사·요금 버전·원가·지원·오류 관리 |

편집기 왼쪽에는 앞면·뒷면 등 면 목록, 텍스트·이미지·바코드·레이어를 둔다. 중앙에는 확대·축소 가능한 캔버스와 자·안전영역을 둔다. 오른쪽에는 선택 객체의 내용·글꼴·크기·좌표와 검수 결과를 둔다. 상단에는 저장 상태, 2D·3D 전환, 잔액, 출력 버튼을 둔다. 도움말은 현재 오류를 수정할 위치로 연결한다.

처음 사용자는 포장 선택 → 규격 확인 → 상품 정보 → 시안 선택 → 직접 수정 → 모든 면 확인 → 출력 순서로 진행한다. 구조를 모르고 디자인을 시작할 수는 있지만 규격이 미확정이면 검토용으로만 저장한다. 내보내기 버튼은 결과 유형과 차감량을 명시한다. 예시는 “시안 3장 만들기 · 30크레딧”, “제작 파일 준비 · 40크레딧”, “같은 항목 다시 출력 · 0크레딧”이다.

모든 화면에 로딩·빈 상태·오류·권한 부족·잔액 부족을 구현한다. AI가 실행 중이어도 저장한 프로젝트를 열람할 수 있어야 한다. 작업 결과가 늦어지면 재클릭을 유도하지 말고 상태와 취소 가능 여부를 보여 준다. 저장되지 않은 변경으로 이동할 때 경고한다.

## 6 상품 정보와 편집 데이터

상품의 기초 정보와 장면에 그려지는 객체를 분리한다. product는 브랜드와 기본 상품, variant는 맛·용량 등 판매 단위, scene은 각 면의 배치와 스타일을 보관한다. 제품명·중량·원재료·알레르기·제조사·보관 문구는 사용자가 제공하고 확정한다. 입력값이 없으면 비어 있거나 “확인 필요”로 남긴다.

AI는 미확정 문구의 초안을 제안할 수 있으나 고객 확정값을 덮어쓰지 않는다. 영양 수치·효능·원산지·인증 마크를 제품 이미지에서 추정해 확정 입력하지 않는다. 법정 표시의 자동 적합성 보장은 제공하지 않는다. 출력을 위한 확인 목록은 고객이 입력한 항목과 제조사 요구조건을 기준으로 한다.

텍스트 객체의 binding_key는 상품의 어떤 필드에 연결됐는지 기록한다. 필드값 변경 시 연결된 객체만 바뀌고 사용자가 수동으로 분리한 객체는 유지한다. 변경 전후 값을 보여 주고 확인 후 반영한다. 제품명 수정과 용량·바코드 변경을 구별해 과금상 새 제작 항목인지 판단한다.

### 6 1 공통 장면 형식

영구 데이터는 Konva 전용 직렬화 결과가 아니라 독립 JSON Schema로 관리한다. Konva와 Three.js는 이를 표시하는 어댑터다. schema_version, template_version_id, geometry_hash, product_variant_id, revision_id, print_profile_version_id를 기록한다. 브라우저, 서버 출력과 3D가 같은 리비전을 읽는다.

| 객체 | 필수 데이터 | 저장과 출력 규칙 |
|---|---|---|
| 공통 | id, type, face_id, x_mm, y_mm, width_mm, height_mm, rotation_deg, z_index | 좌상단 원점, 회전 중심 명시, 배열 순서와 별개 정렬 |
| 텍스트 | text, font_id, font_size_pt, line_height, align, color, binding_key | 원문·줄바꿈·조합 문자 보존, 출력시 글꼴 검증 |
| 이미지 | asset_id, crop, opacity, source_pixel_size | 원본 유지, 상대 자르기 좌표, 유효 PPI 계산 |
| 바코드 | symbology, data, module_mm, bar_height_mm, quiet_zone | 임의 가로·세로 늘리기 금지, 서버 재생성 |
| 도형 | path 또는 기본 도형, fill, stroke | 허용 도형만 처리, 임의 스크립트 불가 |
| 가공 요소 | 별도 geometry 객체 | 일반 아트워크 편집으로 칼선·구멍 이동 불가 |

좌표 저장은 mm를 소수 넷째 자리까지 정규화한다. 계산 중에는 충분한 정밀도를 유지하고 출력 직전 한 번만 반올림한다. 화면 확대율이나 기기 픽셀 비율을 원본 치수에 곱해 저장하지 않는다. PDF는 1mm = 72/25.4pt로 변환한다. 내부 계산의 정확도 목표와 실제 제조 허용오차를 구분한다.

각 리비전은 불변 스냅샷이다. 편집 중 자동 저장은 초안 버전을 갱신하고, 생성·검수·출력 시 스냅샷을 만든다. 저장 요청은 base_revision을 보내며 충돌 시 HTTP 409와 서버 버전을 반환한다. 여러 좌석이 같은 프로젝트를 열면 편집 잠금과 읽기 모드로 충돌을 예방하고, 잠금 만료 후에도 버전 검사를 생략하지 않는다.

## 7 한글 편집기 구현

Konva 텍스트 위에 HTML textarea를 겹쳐 실제 입력을 처리한다. 캔버스 자체 키 이벤트로 한글 조합을 흉내 내지 않는다. compositionstart·compositionend와 isComposing을 처리하고 조합 중 Enter를 완료나 단축키로 가로채지 않는다. 이 방식의 기반 예시는 Konva 공식 문서에서 확인할 수 있다. [S2]

직접 수정, 복사·붙여넣기, 다중 행, 글꼴 크기, 자간, 행간, 정렬, 회전, 정렬 맞춤, 객체 잠금, 레이어 순서, 실행 취소·다시 실행을 제공한다. Shift+Enter와 Enter의 역할을 일관되게 정하고 조합 입력 시험을 한다. 본문을 숨긴 상태에서 PDF에만 남거나 그 반대가 되지 않도록 visible과 print_enabled를 구분한다.

글꼴은 서버와 웹에 같은 파일 버전을 사용하며 라이선스와 SHA-256을 관리한다. 누락된 글리프가 있으면 출력 전에 오류를 표시한다. 브라우저가 임의 대체 글꼴로 표시한 상태를 정상 결과로 저장하지 않는다. 검수용 기준 폰트는 배포와 임베딩이 허용된 한글 폰트로 선정한다.

줄바꿈은 텍스트 박스 폭과 명시적 줄바꿈을 함께 반영한다. 서버는 같은 폰트로 줄바꿈과 글리프 폭을 재계산하고 확인 가능한 출력 미리보기를 만든다. 렌더러별 줄바꿈 차이가 허용범위를 넘으면 제작 출력을 차단한다. 전체 캔버스를 저해상도 PNG로 저장해 PDF에 붙이는 방식으로 이 문제를 숨기지 않는다.

자동 저장은 입력 안정화 후 실행하며 “저장 중”, “저장됨”, “연결 끊김”을 구분한다. IndexedDB에 임시 복구본을 둘 수 있지만 서버 저장 여부와 혼동하지 않게 한다. 네트워크 복구 후 충돌 없는 경우에만 재전송한다.

## 8 칼선과 포장 구조 엔진

### 8 1 승인 가능한 템플릿

템플릿은 구조 종류, 제조사, 버전, 도면 출처, 사용권, 허용 치수 범위, 재질, 실링·접힘·지퍼·노치·구멍 규칙, 인쇄 가능영역, 면 매핑과 승인 증빙을 포함한다. 상태는 demo → draft → review → approved → revoked로 관리한다. approved 전환은 운영 관리자만 가능하고 증빙 파일과 승인 담당자·날짜를 필수 기록한다.

제조사 원본 PDF·SVG는 참고 자료로 수집할 수 있다. 임의 PDF를 올리면 즉시 정확한 가변 칼선이 되는 것으로 구현하지 않는다. 초기에는 관리자가 치수와 면을 매핑하고 검토하는 등록 화면을 제공한다. 도면 변경 시 새 버전을 만들고 이전 프로젝트에 자동 덮어쓰지 않는다.

예시 템플릿은 애플리케이션 동작 시험용으로 직접 만든다. “데모 구조 · 제조사 미승인”을 화면과 검토 파일에 표시한다. 예시 치수·블리드·실링값을 실제 제조 규칙으로 소개하지 않는다. 승인 자료가 없으면 개발 검수는 진행하되 제작용 출력 게이트는 닫아 둔다.

### 8 2 봉투의 치수

3면 실링형은 완성 폭·높이와 좌우·하단 실링을 각각 기록한다. 상단 개구부·후속 열접착 구간과 지퍼·노치 여부도 템플릿에 따른다. 인쇄 영역은 단순히 완성 사각형 전체와 같다고 가정하지 않는다.

스탠드형은 완성 폭·높이, 펼친 바닥 폭과 접힌 반폭의 의미를 분명히 정의한다. 사용자에게 바닥 폭을 그림과 이름으로 안내하고 제조사 정의에 따라 변환한다. 앞·뒤·바닥이 단일 원단에 이어지는지 별도 패널인지 템플릿이 결정한다. 모든 제조사에 “폭 두 배와 바닥을 더하면 전개도”라는 공식을 공통 적용하지 않는다.

기본 입력은 mm다. cm를 선택하면 숫자와 단위를 함께 표시하고 정확히 변환한다. 단위 없이 “23×31”만 입력하면 해석을 요구한다. 임의로 230×310mm 또는 23×31mm를 선택하지 않는다.

### 8 3 계산과 검수

서버가 치수 입력을 검증하고 cut·fold·seal·safe·bleed·hole·no_print 영역을 계산한다. 프론트는 서버 결과를 기준으로 표시한다. 실시간 조작용 프론트 계산이 있더라도 저장·검수·출력 시 서버가 다시 계산한다. geometry_hash에 템플릿 버전과 정규화한 입력을 포함한다.

벡터 경로의 닫힘, 자기 교차, 음수·과대 치수, 빈 인쇄영역, 면의 중첩, 구멍과 실링의 충돌을 검사한다. 허용 범위를 벗어난 값은 자동 축소하지 말고 해당 입력을 표시한다. 텍스트·바코드 등 중요 객체가 안전영역을 침범하면 오류를 내고 배경의 블리드 확장은 별도로 허용한다.

원형 걸이 구멍은 지름과 중심 위치로 관리한다. 제조사별 허용영역·최소 간격을 만족할 때만 적용한다. 구멍의 커팅 경로와 구멍 주변 인쇄 금지영역을 생성하고 앞·뒤·3D에서 같은 위치를 사용한다.

### 8 4 박스 확장

박스 구조 1종을 확정하면 외경·내경 기준과 재질 두께·접착 여유를 템플릿에 기록한다. front, back, left, right, top, bottom의 6면 및 glue_tab·flap을 별도로 관리한다. 접힘 방향과 3D 조립 변환을 도면 버전과 함께 저장한다. 박스 P5 단계도 도면 승인 전에는 데모 출력만 허용한다.

## 9 바코드와 전체 면 미리보기

EAN-13은 13자리 숫자와 체크 숫자를 검증한다. 앞 12자리의 홀수 위치 합과 짝수 위치 합의 3배를 더한 값에서 체크 숫자를 계산한다. 기존 13자리의 마지막 숫자가 틀리면 오류를 설명하고 사용자의 확인 없이 상품 번호를 바꾸지 않는다. 선행 0을 잃지 않도록 문자열로 저장한다.

GS1 번호의 정식 부여와 바코드 이미지를 만드는 것은 별개다. 제품이 보유한 번호를 고객이 입력하고 사용권을 확인하게 한다. 체크 숫자가 맞다고 정식 발급·상품 소유·인쇄 품질이 확인된 것으로 표시하지 않는다. [S4]

검증된 바코드 인코더로 SVG 또는 PDF 벡터를 생성한다. 모듈 폭, 바 높이, 좌우 여백, 번호 표시, 배경색을 인쇄 프로파일에서 제한한다. 가로만 압축하거나 바 높이를 임의 축소하는 자유 변형은 막는다. 실링·접힘·구멍·투명 창·곡률이 큰 영역과의 충돌을 검사한다. 최종 파일의 디지털 디코딩 검사와 실물 판독 검사를 각각 기록한다.

3D는 저장된 동일 리비전의 각 면을 텍스처로 사용한다. AI가 별도로 그린 포장 목업을 편집 결과의 정확한 3D라고 표시하지 않는다. 앞·뒤·바닥을 클릭해 2D로 이동하고 아직 확인하지 않은 면을 표시한다. 봉투의 주름과 내용물 팽창은 시각화 가정이며 실제 가공·충전 적합성 검증을 대신하지 않는다.

검수용 모드는 면 이름과 방향 화살표를 표시한다. 뒷면이 좌우 반전되거나 바닥의 글자가 뒤집히는 문제를 이 모드로 시험한다. WebGL을 사용할 수 없으면 2D 전체 면 보기로 전환하고 데이터 손실 없이 작업을 계속한다.

## 10 AI 생성과 수정

### 10 1 입력과 결과

AI 입력에는 디자인 지시, 포장 종류, 면 비율, 브랜드 색상, 참조 자산과 텍스트를 놓을 비워 둔 영역을 포함한다. 상품 표시 문구와 바코드는 결과 이미지에 합성하지 않는다. 제품 사진은 고객 원본을 별도 이미지 객체로 배치하고 외형 변경 요청이 없는 한 AI가 제품 형태를 새로 그리지 않게 한다.

생성 결과는 원본 파일, 실제 픽셀 크기, 제공자, 모델 ID, 품질 옵션, 프롬프트 버전, 참조 자산, 작업 ID, 제공자 요청 ID, 사용량·추정 원가를 저장한다. 시안 3장은 독립 하위 작업으로 관리해 부분 성공을 정산한다. 이미지 선택은 현재 프로젝트의 배경 자산 연결만 바꾼다.

수정은 원본을 보존한 새 자산으로 생성한다. 사용자가 선택한 영역 밖의 내용이 항상 보존된다고 보장하지 않는다. 제공자 마스크 기능의 유무와 의미를 capability로 관리하고 지원하지 않는 옵션을 전송하지 않는다. 수정 결과를 비교한 뒤 사용자가 적용한다.

### 10 2 API 어댑터

ImageProvider는 generate, edit, get_capabilities의 계약을 구현한다. production 설정에서는 실제 API 키와 허용 모델이 있어야 실행한다. 개발용 fixture 어댑터는 자체 제작한 예시 이미지를 반환하고 “데모 이미지”를 표시한다. 운영 환경에서 fixture 사용을 금지하는 시작 검사를 둔다.

OpenAI 공식 문서는 이미지 생성·편집용 Image API와 대화형 작업의 Responses API를 구분한다. 초기에는 단일 생성·편집 요청을 Image API로 연결하고, 대화형 편집이 필요해질 때 상태 관리 방식을 확장한다. 모델 ID·품질·해상도는 관리 설정과 환경변수로 지정한다. gpt-image-2를 사용할 경우 해당 모델의 지원 파라미터를 확인하고 input_fidelity를 임의로 전달하지 않는다. [S1]

사용자 화면의 “표준”과 “고해상도”는 내부 capability 설정에 연결한다. 픽셀 수만으로 인쇄 가능을 판정하지 않는다. effective_ppi = 실제 사용 픽셀 수 ÷ 배치 길이의 인치 값을 X·Y축별로 계산한다. 확대 보간으로 픽셀을 늘렸다고 원본 세부 해상도가 확보된 것으로 표시하지 않는다.

오류는 인증, 한도·잔액, 요청 형식, 콘텐츠 처리 불가, 일시 장애, 시간 초과로 구분한다. 일시 오류만 제한적으로 재시도하고 이미 성공한 결과를 다시 생성하지 않는다. 결과가 불명확한 타임아웃은 상태를 확인하고 공급자 원가와 사용자 차감을 각각 처리한다.

## 11 출력과 제작 파일 검수

### 11 1 출력 유형

| 유형 | 내용 | 허용 조건과 차감 |
|---|---|---|
| 화면 미리보기 | 화면용 이미지와 3D | 0크레딧 |
| 검토용 묶음 | 실제 크기 확인 PDF, 미리보기, 미확인 항목 | 데모·미승인 허용, 검토용 표시, 0크레딧 제안 |
| 제작용 묶음 | 승인 사양 PDF, 면별 미리보기, 작업 정보, 검수 기록 | 승인·권한·검수 통과, 최초 40크레딧 |
| 같은 항목 재출력 | 수정 리비전의 같은 묶음 | 검수 재실행, 0크레딧 |

검토용 0크레딧은 이번 구현의 운영 기본안이다. 남용 방지를 위해 횟수 과금 대신 요청 속도와 동시 실행 수를 제한한다. 체험의 검토 파일에는 “검토용 · 제작 사용 불가”를 표시한다. 출력 유형을 바꿔 체험에서 제작 파일을 우회 획득할 수 없도록 서버에서 확인한다.

제작용 묶음은 production.pdf, preview.png, job-ticket.json, job-ticket.pdf, preflight.json, manifest.json을 포함한다. manifest에는 파일 해시, 프로젝트·리비전·템플릿·프로파일 버전, 생성 시각을 기록한다. 편집용 데이터는 별도 프로젝트 다운로드에 제공하고 원본 자산과 폰트의 재배포 권한을 확인한다.

### 11 2 출력 엔진과 프로파일

PDF 어댑터는 ReportLab·pikepdf 등으로 기본 벡터 PDF와 메타데이터를 구현할 수 있다. 라이브러리 선택만으로 PDF/X, 별색, 오버프린트, 화이트 잉크 분판이 완성됐다고 간주하지 않는다. 출력 엔진의 실제 지원 기능을 capability 목록으로 등록하고 프로파일의 요구 기능과 비교한다.

인쇄 프로파일은 제조사, 재질, 템플릿 버전, 필요한 PDF 규격, ICC 프로파일, CMYK·별색·화이트 인쇄, 폰트 처리, 레이어·분판 이름, 블리드, 최소 PPI, 바코드 치수, 허용오차와 검증 도구를 포함한다. 특정 업체의 블리드 수치를 모든 봉투의 기본값으로 사용하지 않는다. ePac의 실제 안내에서도 색상·폰트·레이어·투명 부분 처리가 각각 요구된다. [S3]

서버는 아트워크와 바코드를 벡터·원본 이미지로 합성하고 칼선 등 구조 요소를 프로파일이 요구하는 방식으로 분리한다. 구조 안내선이 의도치 않게 인쇄 아트워크에 섞이지 않게 한다. 검토용 PDF의 치수선과 검토 표시를 제작용에 그대로 넣지 않는다. PDF의 페이지·TrimBox·BleedBox는 원본 규격과 일치하도록 계산한다.

RGB 이미지의 CMYK 변환은 지정 ICC와 검증된 변환 경로로 수행한다. 파일 메타데이터를 CMYK로 적거나 PDF 확장자를 바꿔 변환을 대신하지 않는다. PDF/X 검증이 요구되는 프로파일은 적합성 확인이 가능한 도구와 결과를 연결해야 활성화한다. 미지원이면 그 조건의 제작 출력을 차단하고 지원 가능한 검토용 결과를 제공한다.

### 11 3 제작용 출력 게이트

서버는 다음 조건을 모두 확인한다.

1. 계정과 역할이 출력에 적합하고, 제작 항목과 리비전이 고정돼 있다.
2. 템플릿·프로파일·재질 조합이 approved이며 승인 철회가 없다.
3. 입력 규격, 모든 면, 고객 확정 문구, 바코드와 구멍이 검수를 통과했다.
4. 실제 출력 엔진이 색상·글꼴·레이어·PDF 규격을 처리할 수 있다.
5. 글리프 누락, 중요 객체 잘림, 해상도 부족, 구조 충돌 등 blocker가 없다.
6. 사용자가 현재 리비전의 내용·번호·전체 면 확인을 마쳤다.
7. 최초 출력이면 필요한 크레딧을 예약할 수 있다.

검수 이후 리비전이 바뀌면 이전 통과 결과를 재사용하지 않는다. 출력 완료 직전 승인 상태를 다시 확인해 철회된 조건의 결과 공개를 막는다. 제작 파일 생성 성공과 제조사 최종 입고 승인 상태는 별개 필드로 표시한다. 승인된 PDF도 생산 직전 확인 절차를 생략했다고 표현하지 않는다.

## 12 월 구독과 크레딧 정책

아래 값은 최종 보고서에서 이어받은 초기 제안값이다. 기준 파일은 config/pricing.seed.json이며 운영 반영에는 별도 정책 버전을 만든다. 구독 기간·한도·차감량은 서버가 계산하고 견적 시점의 버전을 기록한다. 크레딧은 작업 사용량 단위이며 원화 잔액이나 제공자 API 토큰과 동일하지 않다.

### 12 1 요금제와 추가 서비스

| 구분 | VAT 별도 원 | VAT 포함 원 | 지급 크레딧 | 좌석과 조건 |
|---|---:|---:|---:|---|
| Starter 월 구독 | 49,000 | 53,900 | 500 | 1명 |
| Pro 월 구독 | 99,000 | 108,900 | 1,500 | 3명 공용 |
| Partner 월 구독 | 249,000 | 273,900 | 4,500 | 5명 공용과 고객사 공간 |
| 추가 충전 500 | 39,000 | 42,900 | 500 | 구매 후 12개월 |
| 추가 충전 1000 | 69,000 | 75,900 | 1,000 | 구매 후 12개월 |
| 체험 | 0 | 0 | 30 | 가입자 1회와 14일 |
| 사람의 파일 검토 | 50,000 | 55,000 | 지급 없음 | 상품당 검토·보완 확인 각 1회 |

월 크레딧은 팀 전체에 한 번 지급한다. 좌석을 추가했다고 월 지급량을 곱하지 않는다. Partner의 고객사별 공간은 같은 조직 안의 분리된 작업 공간이며 각 공간에 구독 크레딧을 다시 지급하지 않는다. 사용자 접근권한은 공간별로 제한할 수 있어야 한다.

체험은 표준 시안 생성·수정 합계 3장에 사용할 수 있는 30크레딧이며 제작용 출력과 고해상도 생성은 제공하지 않는다. 별도 카드 등록이나 자동 유료 전환은 없다. 유료 플랜에서는 장수·상품 수를 별도 과금 한도로 중복 제한하지 않는다. 다만 저장공간·업로드 크기·동시 작업 등 기술적 제한은 이용 안내에 명시할 수 있다.

유료 시험 모집용 첫 달 Pro 99,000원과 도입 지원 100,000원을 별도 주문 항목으로 관리할 수 있게 한다. 도입 지원은 일회성이며 매월 재결제하지 않는다. 인쇄·샘플·맞춤 구조·사람의 검수 비용을 일반 크레딧에 암묵적으로 포함하지 않는다. 초기 구현의 별도 검수는 신청·견적·상태 관리부터 제공하고 자동 전문가 배정은 후속 기능이다.

### 12 2 작업별 차감

| 작업 코드 | 크레딧 | 성공 단위 |
|---|---:|---|
| image.generate.standard | 10 | 사용 가능한 이미지 1장 |
| image.edit.standard | 10 | 사용 가능한 수정 이미지 1장 |
| image.generate.high | 20 | 설정된 고해상도 이미지 1장 |
| export.production.first | 40 | 새 제작 항목의 완성된 파일 묶음 |
| editor.manual | 0 | 문구·배치·바코드 직접 수정 |
| preview.all_faces | 0 | 칼선·전체 면·3D 확인 |
| export.production.repeat | 0 | 같은 제작 항목의 재출력 |
| export.review | 0 | 검토용 묶음인 구현 추가 제안 |

고해상도 수정은 별도 차감량이 확정되지 않았으므로 초기에는 비활성화한다. 고해상도 생성의 모델·품질·크기는 실제 원가를 확인한 뒤 활성화한다. 표준 10크레딧 요청을 사용자 동의 없이 더 비싼 종류로 전환하지 않는다.

예시 작업은 시안 3장 30 + AI 수정 1장 10 + 최초 제작 출력 40 = 80크레딧이다. 같은 항목의 문구 수정과 재출력은 0이다. 500크레딧으로 이 구성 6건 후 20, 1,500으로 18건 후 60, 4,500으로 56건 후 20이 남는다. 이는 특정 작업 구성의 예이며 상품 건수를 별도로 보장하는 플랜이 아니다.

### 12 3 제작 항목 식별

과금 항목 production_item은 프로젝트 ID와 분리한다. 사용자가 같은 프로젝트를 복제하거나 이전 문구로 복원했다고 최초 출력 비용이 다시 발생하면 안 된다. 반대로 화면의 상품 이름만 유지한 채 용량이나 번호를 바꾸면 새 제작 항목으로 판단해야 한다.

fingerprint는 tenant_id, brand_id, 안정된 product_variant_id, 정규화한 내용량과 단위, 바코드 종류와 번호, 구조의 billing_family_key, 완성 폭·높이·바닥·깊이 및 과금상 중요한 가공 규격으로 계산한다. JSON 키 순서·단위 표기 차이가 해시에 영향을 주지 않게 canonical JSON을 사용한다. 상품명 문자열만으로 상품을 식별하지 않는다. 동일 variant의 제목 오타 수정은 새 상품이 아니다.

새 상품·다른 용량·다른 바코드·다른 완성 규격·다른 구조는 새 항목이다. 같은 항목의 문구·배치·인쇄 수량 변경과 파일 재다운로드는 무료다. 템플릿 버전의 버그 수정이나 인쇄 프로파일 교체는 물리적 구조가 같으면 같은 billing_family_key로 묶어 중복 과금하지 않는다. 물리 구조가 바뀌면 새 항목으로 견적을 다시 받는다.

바코드 추가·삭제, 구멍 규격, 재질 변경처럼 원본 보고서가 세부적으로 정하지 않은 변경은 config의 production_identity_policy로 관리한다. 초기 기본안은 바코드 값·가공 규격 변경은 새 항목, 인쇄 프로파일·색상 보정·재질명만 변경은 같은 구조일 때 재출력이다. UI는 예상 변경사항과 차감량을 먼저 보여 준다. 정책 변경을 과거 지급된 출력 권한에 소급 적용하지 않는다.

최초 출력 성공 시 production_entitlement를 생성한다. unique(tenant_id, fingerprint)를 DB로 보장한다. 동일 항목을 동시에 두 번 요청하면 한 요청에 연결하거나 하나만 수행한다. 과거 제작 파일이 있어도 템플릿이 철회됐거나 문구가 바뀌었다면 검수를 다시 거쳐야 한다. 무료 재출력 권한은 검수 면제권이 아니다.

### 12 4 지급과 유효기간

월 지급분은 최초·갱신 결제가 성공한 주기에서만 지급하며 다음 결제 주기 시작 시각에 만료한다. 이월하지 않는다. 추가 구매분은 구매일부터 12개월, 체험은 14일이다. 조회와 사용 시 expires_at > now 조건을 검사한다. 정기 만료 배치가 지연돼도 만료 크레딧을 사용할 수 없어야 한다.

작업에 사용 가능한 종류의 잔액 중 만료가 빠른 순서로 배정한다. 같은 만료일이면 지급 시각과 ID 순서로 결정한다. 유료 제작 출력에 체험 크레딧을 사용하지 않는다. 체험, 월 지급, 구매, 추천 보너스, 오류 보상분을 원장에서 구분한다.

상향 변경은 남은 주기의 비율로 요금 차액과 크레딧 차액을 함께 계산한다. 초기 제안은 남은 초 ÷ 전체 주기 초를 비율로 쓰고, 원화는 원 단위 반올림, 크레딧은 정수 내림이다. 결제 전에 양쪽 금액과 지급량을 보여 주고 결제 성공 후 같은 주기에만 반영한다. 하향 변경은 다음 갱신일부터 적용한다. 이미 예약된 작업 견적은 바꾸지 않는다.

해지는 다음 결제를 중단하며 현재 유료 기간은 유지한다. 기간 종료 후 팀 좌석은 소유자 1명으로 축소하고 다른 사용자 접근을 해제한다. 프로젝트와 고객사 공간의 데이터는 소유자가 열람·편집할 수 있게 보존한다. 남은 구매 크레딧은 유효기간까지 기본 1인 계정에서 사용할 수 있고, 유료 이용 이력이 있는 계정의 제작 출력 권한도 그 범위에서 유지한다. 이 처리를 위해 구독 상태와 자산·출력 이용 권한을 분리한다.

갱신 결제가 실패하면 해당 주기의 새 크레딧을 지급하지 않는다. 남아 있는 유효 구매분은 그대로 유지한다. 추가 충전 구매는 초기에는 활성 구독 계정에 제공하되, 해지 후 보유 구매분의 사용은 차단하지 않는다.

제작 파일은 유료 기간 종료 후 90일 또는 구매 크레딧 유효기간 중 더 늦은 날짜까지 보관하는 안을 적용한다. 해당 작업을 다시 열기 위한 원본·장면도 함께 보관한다. 만료 예정 알림과 다운로드 경로를 제공한다. 삭제 요청, 미완료 작업, 분쟁·보관 사유는 별도의 보존 상태로 처리하며 임의 정리 배치가 활성 자산을 지우지 않게 한다.

## 13 크레딧 원장과 실패 복구

### 13 1 예약과 확정 차감

요청 접수에서 전액을 즉시 확정 차감하지 않는다. 작업 비용을 예약하고 사용 가능한 결과가 저장됐을 때 확정한다. 원장은 append-only로 관리하고 관리자도 과거 행을 수정·삭제하지 못하게 한다. 정정은 사유가 있는 반대 전표로 남긴다.

원장 유형은 GRANT, RESERVE, CAPTURE, RELEASE, EXPIRE, COMPENSATE, ADJUSTMENT다. 금액과 크레딧은 정수다. 잔액은 사용 가능, 예약 중, 사용 완료, 만료를 구분한다. 총 보유량에서 예약량을 두 번 차감하는 UI 계산을 하지 않는다.

1. 서버가 상품·리비전·정책 버전을 고정해 5분 유효 견적을 만든다.
2. 실행 요청은 quote_id와 Idempotency-Key를 보낸다. 클라이언트의 크레딧 숫자는 무시한다.
3. DB 트랜잭션에서 조직 지갑과 대상 버킷을 고정 순서로 잠근다. 만료와 권한을 재검사한다.
4. 충분한 잔액을 FEFO로 예약하고 작업·할당내역·outbox를 함께 저장한다.
5. 커밋 후 outbox 소비자가 큐로 전달한다. 큐 중복 전달에도 같은 작업만 실행한다.
6. worker가 결과를 임시 저장하고 파일 읽기·해시·검수를 확인한다.
7. DB 트랜잭션으로 결과를 공개 상태로 바꾸고 필요한 예약분만 CAPTURE한다.
8. 실패하거나 제공하지 못한 결과의 예약은 RELEASE한다. 사유와 사용자 안내를 기록한다.

DB 저장과 객체 저장은 한 트랜잭션이 아니므로 임시 자산·manifest·공개 상태를 분리한다. 파일 업로드만 성공하고 DB가 실패하면 사용자를 청구하지 않고 orphan 자산을 정리한다. DB에 완료가 기록됐는데 저장 파일이 없으면 장애 상태로 전환해 복구 또는 보상한다.

### 13 2 부분 성공과 중복

시안 3장을 요청해 최종적으로 2장만 사용 가능하면 30 예약 중 20만 확정하고 10을 반환한다. 자동 재시도가 진행 중이면 실패분 10의 예약을 유지하며 최초 예약 한도 안에서 정산한다. 하위 작업이 최종 실패로 종료된 시점에 예약을 반환한다. 이미 종료 후 예약을 반환한 작업은 뒤늦은 공급자 응답만으로 추가 차감하지 않는다. 사용자가 새 작업을 승인했을 때만 새 예약을 만든다.

실행 시작 전 취소는 전액 반환한다. 실행 후 취소는 기본적으로 제한하고 작업은 백그라운드에서 완료할 수 있음을 표시한다. 이미지가 정상 제공됐지만 사용자가 미적 이유로 다른 시안을 원하면 새 생성이다. 생성 실패, 접근 불가 파일, 잘못된 출력 형식 등 시스템 오류는 무차감 또는 보상이다.

Idempotency-Key는 tenant·요청 종류·정규화 요청 해시와 묶어 보관한다. 같은 키와 같은 본문에는 기존 작업을 반환하고, 같은 키와 다른 본문은 409로 거절한다. 제공자 재시도는 내부 attempt_id를 새로 만들되 사용자 작업 ID와 차감 이벤트는 유지한다.

### 13 3 만료와 작업 중단

예약 당시 유효했던 크레딧은 짧은 작업 완료 유예 안에서 확정할 수 있다. 만료 배치는 사용 가능 잔액만 만료시키고 예약분을 중복 만료시키지 않는다. worker heartbeat와 lease를 사용하고 초기 job hard timeout은 30분, 큐 대기 상한은 10분으로 제안한다. 작업 종류에 맞게 운영 설정으로 조정한다.

실패했는데 원래 버킷이 만료돼 반환분을 쓸 수 없다면 원래 이용 범위를 유지한 보상 버킷을 만든다. 초기 기본안은 원래 만료일과 반환 시점 7일 후 중 늦은 날짜다. 이 7일은 구현 제안값이며 오류 복원으로 체험이 제작 출력 권한으로 바뀌지 않게 한다.

작업 프로세스가 죽거나 결과가 불명확하면 reconciliation 작업이 provider_request_id, 저장 자산, 원장 상태를 조사한다. 예약 상한 시간이 끝난 요청은 반환하고 뒤늦은 결과에 자동 청구하지 않는다. 일괄적으로 모든 오래된 작업을 다시 생성하는 방식은 금지한다. 고객에게 작업 ID와 복원 내역을 제공한다.

## 14 정기결제와 요금 변경

PG 어댑터는 등록·첫 결제·갱신·충전·결제 조회·취소의 공통 계약을 갖는다. 국내 기본 구현 후보는 Toss Payments다. 실제 계약과 테스트·라이브 키는 설정으로 분리하고 카드 원문·CVC를 자체 DB나 로그에 저장하지 않는다. PG가 제공하는 결제창에서 인증하고 서버가 billingKey와 customerKey를 암호화해 관리한다.

Toss의 정기결제는 상점이 일정에 맞춰 승인 API를 호출하는 구조다. 결제 성공 웹훅만 기다려 구독 크레딧을 지급하는 구현은 하지 않는다. 서버 승인 응답을 검증해 반영하고, 불명확한 타임아웃은 결제 조회로 확인한다. 지원되는 웹훅은 누락 보완과 상태 동기화에 사용한다. [S5] [S6]

구독에는 billing_anchor, timezone, current_period_start, current_period_end, cancel_at_period_end를 둔다. 기본 서비스 시간대는 Asia/Seoul이며 DB 시각은 UTC로 저장한다. 31일 가입자가 짧은 달을 지나는 경우 말일로 보정하고 원래 anchor_day를 보존한다. 해당 주기는 unique(subscription_id, period_start)로 한 번만 처리한다.

invoice를 먼저 만들고 스케줄러가 해당 invoice의 고유 order_id로 승인한다. 결제 확인 후 같은 트랜잭션에서 결제 상태, 주기 활성화, 크레딧 지급, outbox 알림을 기록한다. 카드 등록 완료나 브라우저 success URL 방문만으로 결제 완료 처리하지 않는다. 금액·통화·계정·주문 ID와 결제 상태를 서버에서 확인한다.

웹훅은 제공자의 공식 검증 방식과 필요한 결제 재조회로 신뢰성을 확인한다. 임의의 공통 서명 헤더가 존재한다고 가정하지 않는다. provider_event_id 또는 동일성을 입증할 수 있는 이벤트 키에 unique를 걸고, 순서가 바뀐 이벤트가 이미 완료한 정산을 되돌리지 못하게 한다. 원본 결제 기록은 변조하지 않고 환불·취소 이력을 별도로 남긴다.

정기결제 재시도는 동일 청구서에 대해 이뤄지고 중복 승인이 없음을 검증한다. 실패 후 24시간·72시간 재시도는 초기 운영 제안이며 횟수·간격을 설정으로 둔다. 해지 요청 이후 새 주기 청구를 시작하지 않도록 구독과 청구 작업에 동시성 제어를 한다.

환불 규칙은 월 구독과 추가 구매분을 구분해 운영 설정과 이용조건으로 확정한다. 미확정 상태에서는 라이브 결제 게이트를 닫고 모의·PG 테스트로 개발을 마친다. 실제 환불은 사용량 조회, 원장 정정, PG 취소 결과와 연계한다. 이미 소비한 크레딧을 단순 삭제하거나 잔액을 음수로 숨기지 않는다.

추천 보너스는 후속 플래그로 둔다. 최종 보고서의 제안은 첫 유료 결제가 취소 가능 기간을 지난 뒤 100크레딧, 지급 후 30일, 월 3회, 자기 추천 제외다. 확정한 운영 조건이 없으면 자동 지급을 켜지 않는다.

## 15 데이터 모델과 제약

모든 고객 데이터는 tenant_id를 가진다. 조직 경계를 가진 외래키는 tenant_id를 함께 검사해 다른 조직의 자산을 연결할 수 없게 한다. 관리자 역할과 일반 조직 소유자를 분리한다. UUID를 안다고 접근할 수 있는 API를 만들지 않는다.

| 테이블 그룹 | 핵심 필드와 제약 |
|---|---|
| users sessions | 이메일, password_hash, token_hash, 만료, 인증 상태 |
| tenants memberships | 조직, 사용자, owner·admin·editor·viewer 역할, unique 조직·사용자 |
| workspaces workspace_members | Partner 고객사 공간과 사용자 범위 |
| brands products variants | 브랜드 자산, 상품과 변형, 고객 확정 필드·바코드 |
| projects revisions | 조직·공간·상품, base_revision, 불변 scene_json, schema_version |
| assets font_assets | 객체 키, MIME, bytes, hash, 라이선스, 픽셀·폰트 정보 |
| manufacturers templates template_versions | 출처, 구조, 승인 상태, immutable 버전 |
| print_profiles profile_versions | 출력 capability, 재질, 조건, 승인 증빙 |
| model_presets policy_versions | 모델·품질·원가와 차감표, 적용일 |
| subscriptions invoices payments | 주기, 요금 버전, 주문 ID, PG 상태, unique 결제·청구 |
| wallets credit_buckets | 종류, 부여·사용·예약·만료, 음수 불가 |
| credit_ledger reservations allocations | 불변 이벤트, 원 버킷 연결, unique 정산 이벤트 |
| quotes jobs job_attempts | 요청 해시·리비전·정책·상태·공급자 ID |
| production_items entitlements | fingerprint, 최초 성공 결과, unique 조직·항목 |
| preflight_runs exports export_files | 조건 해시, 검수 항목, 묶음 상태와 파일 hash |
| outbox_events webhook_events | 트랜잭션 연결, 전달 상태, 중복 키 |
| audit_events support_cases | 변경자·시간·사유, 반려 원인과 지원 시간 |

원장과 청구, 출력 권한의 unique 제약은 애플리케이션 if문만으로 대체하지 않는다. jobs 결과 공개와 CAPTURE는 같은 DB 트랜잭션에서 이뤄진다. provider 비용 테이블에는 실제 청구 가능 사용량을 사용자 크레딧과 별도로 적재한다. 취소·실패해 사용자에게 무료인 작업의 공급자 원가도 누락하지 않는다.

원장 상세에는 actor_id, operation_key, job_id, invoice_id, amount, bucket_id, reason, created_at을 둔다. 수정 권한은 별도 운영 역할로 제한하고 모든 조정을 감사 기록에 남긴다. 개발용 원장 재생성 스크립트는 실운영에서 실행되지 않게 한다.

## 16 API 계약

아래는 기본 경로다. OpenAPI를 실제 코드에서 생성하고 프론트 타입도 같은 계약에서 생성한다. 공통 응답은 data와 request_id, 오류는 code·message·field_errors·retryable을 가진다. 조직은 인증된 membership으로 결정하고 클라이언트가 보낸 tenant_id만으로 변경하지 않는다.

| 메서드와 경로 | 계약 |
|---|---|
| POST /v1/auth/register 및 login | 가입·인증·세션, 제한과 CSRF 방어 |
| GET /v1/me | 사용자, 조직, 역할, 유효 기능 |
| GET·POST /v1/products | 상품과 변형 등록·조회 |
| GET /v1/templates | 권한 범위의 지원 구조와 승인 상태 |
| POST /v1/geometry/validate | template_version_id와 치수로 geometry·오류 반환 |
| POST /v1/projects | 상품·템플릿·프로파일 참조로 생성 |
| GET /v1/projects/{id} | 현재 초안, 리비전, 필요한 자산 정보 |
| PATCH /v1/projects/{id}/draft | base_revision 검사와 저장, 충돌은 409 |
| POST /v1/projects/{id}/revisions | 불변 장면 스냅샷 생성 |
| POST /v1/assets/uploads | 업로드 세션과 허용 형식·크기 |
| POST /v1/assets/{id}/complete | 서버 파일 검증 후 사용 가능 처리 |
| POST /v1/quotes | 작업 종류·리비전·수량으로 비용·만료·새 항목 판단 |
| POST /v1/jobs | quote_id와 Idempotency-Key로 예약·작업 접수 |
| GET /v1/jobs/{id} | 상태, 하위 결과, 예약·확정·반환량 |
| POST /v1/jobs/{id}/cancel | 시작 전 취소와 반환 |
| POST /v1/preflight | 리비전·프로파일 검수, blocker와 warning |
| POST /v1/exports | 검수·사용자 확인·quote로 출력 작업 생성 |
| GET /v1/exports/{id}/download | 권한·보관기간 확인 후 짧은 URL |
| GET /v1/credits | 사용 가능·예약·종류별 만료와 원장 |
| POST /v1/billing/orders | 서버 가격으로 구독·충전·일회성 주문 생성 |
| POST /v1/billing/confirm | PG 결과 확인, 중복 안전한 정산 |
| POST /v1/billing/change-plan | 상향 견적과 실행, 하향 예약 |
| POST /v1/billing/cancel-renewal | 기간 종료 후 갱신 중단 |
| POST /v1/webhooks/payments/{provider} | 검증·중복 제거·재조회 |
| POST /v1/admin/template-versions/{id}/approve | 증빙 첨부와 승인 감사 기록 |

생성·출력은 HTTP 202와 job_id를 반환하고 초기에 2초 간격 polling으로 상태를 확인한다. SSE는 후속 개선이다. polling이 끊겨도 작업과 정산은 서버에서 지속된다. 401·403·404·409·422·429·503을 목적에 맞게 사용하고 민감한 객체 존재를 다른 조직에 노출하지 않는다.

견적에는 action, requested_units, credit_total, balance_before, balance_after, pricing_version, revision_id, fingerprint, expires_at, input_hash를 포함한다. 견적 후 입력이 바뀌거나 만료되면 새 견적을 요구한다. 잔액 부족이면 어떠한 AI 호출도 시작하지 않는다.

## 17 작업 상태와 운영 안전성

작업 상태는 queued, running, waiting_provider, validating, succeeded, partially_succeeded, failed, canceled, reconciliation_required로 관리한다. 허용된 전이만 서버에서 수행한다. 성공한 하위 결과의 공개·정산 여부는 독립적으로 보관한다. 프론트 화면 이동이나 새로고침이 상태를 초기화하지 않는다.

조직별 동시 AI 작업은 기본 2개, 출력 작업은 1개로 제안하고 큐에 넣는다. 이 제한은 월 제공 장수와 다르다. 하루 비용 상한과 제공자 전체 예산 경보를 운영 설정으로 둔다. 무한 재시도는 하지 않으며 일시 오류의 초기 재시도 상한은 2회다.

파일 업로드는 MIME와 실제 파일 형식을 함께 확인한다. 초기 허용은 PNG·JPEG·WebP와 정화한 SVG이며 참고 도면 PDF는 별도 수집한다. 파일당 20MiB, 프로젝트 자산 합계 200MiB는 기술 기본안이다. SVG의 script·외부 참조·이벤트 속성과 위험한 데이터 URL을 제거하고 PDF 처리기는 격리한다. 고객 자산 URL을 서버가 무제한 가져오는 기능은 막는다.

API 키, 결제 비밀값, 세션 토큰, signed URL을 클라이언트 번들·Git·로그에 넣지 않는다. NEXT_PUBLIC 계열에는 비밀값을 넣지 않는다. 원본 디자인과 출력물은 비공개 저장소에 보관하고 짧은 만료 URL로 제공한다. 공유 링크는 이번 초기 범위에서 제공하지 않고 팀 권한으로 공유한다.

소유자는 결제·좌석·작업 공간을 관리한다. editor는 허용된 공간의 편집·출력·크레딧 소비를 수행하고 viewer는 열람·허용된 기존 파일 다운로드만 한다. 조직 owner라고 운영 템플릿을 승인할 수 없고, 운영 관리자라도 고객 자산을 이유 없이 열람하지 않도록 지원 접근 기록을 남긴다.

## 18 관리 화면과 성과 측정

관리자는 제조사별 승인 조합, 템플릿 버전, 모델·출력 capability, 가격 정책, 오류와 크레딧 조정을 관리한다. 승인 상태의 변경은 즉시 신규 출력 게이트에 반영한다. 이미 완료된 파일은 기존 버전을 보존하되 “해당 조건 승인 철회” 상태와 사유를 알릴 수 있게 한다.

| 이벤트 | 기록 목적 |
|---|---|
| signup_completed와 trial_granted | 가입과 체험 중복 확인 |
| project_created와 brief_completed | 작업 시작과 정보 입력 완료 |
| generation_succeeded와 generation_failed | 성공량과 원가·실패 원인 |
| text_edited와 all_faces_reviewed | 직접 수정·전체 면 확인 |
| preflight_failed와 production_export_succeeded | 기술 반려와 출력 성공 |
| credits_insufficient와 topup_paid | 잔액 부족 이탈과 충전 |
| subscription_paid와 renewal_paid | 최초 결제와 유지율 구분 |
| printer_accepted와 printer_rejected | 실제 첫 입고 결과 |

이벤트는 tenant_id·project_id·job_id·revision_id·시간·정책 버전을 연결한다. 광고 분석에는 디자인 원문이나 고객 상품 문구를 보내지 않는다. UTM·유입 채널·체험·최초 결제를 연결하고 갱신이나 추가 충전을 신규 고객으로 중복 집계하지 않는다.

파일 준비 시간은 첫 프로젝트 생성부터 첫 정상 제작 파일 생성까지의 경과 시간과 실제 편집 시간을 구분한다. 입고 통과율은 제조사 회신이 있는 제출 건 중 기술적 반려가 없는 건으로 산정하며 미적 선호 수정은 따로 기록한다. 유료 고객 5곳·실제 작업 10건, 첫 10건의 기술적 입고 90%는 최종 보고서의 검증 목표이며 제품 보장 문구가 아니다.

원가 화면에는 제공자 요청별 실제 또는 추정 API 비용, 실패 재시도, 저장·출력, 지원 시간, 결제 수수료를 구분한다. 이미지 한 장 200원은 보고서의 계산 가정이므로 운영 상수로 쓰지 않는다. 표준·고해상도 프리셋의 원가를 실측해 10·20크레딧 체계가 유지 가능한지 판단한다.

랜딩페이지는 “상품명·용량·바코드를 직접 수정하고 인쇄 의뢰 파일을 준비하세요”를 중심으로 구성한다. 지원 조건, 월 결제 총액, 크레딧 예시, 추가 충전, 제조사 확인 필요를 안내한다. 매출·절감액·모든 인쇄소 호환을 검증 없이 보장하지 않는다. 광고 예산 설정·집행은 플랫폼 개발 완료 조건이 아니다.

## 19 Codex 개발 단계와 산출물

아래 순서는 개발 의존관계다. 날짜를 확정한 일정이 아니며 각 단계의 검수 통과 후 다음 단계로 진행한다. 매 단계에서 기능을 실제 실행하고 docs/progress.md에 완료·미완료·재현 방법을 갱신한다.

| 단계 | 구현할 내용 | 통과 기준 |
|---|---|---|
| P0 기반 | 저장소, 환경변수, DB, 계정·조직, 파일 어댑터, mock AI·결제, 계약 | 외부 키 없이 시작·가입·저장·재시작 가능 |
| P1 편집 흐름 | 3면 실링 데모, 상품, 이미지·한글 레이어, 저장·복구, 검토 PDF | AC01부터 AC06과 AC11의 검토용 |
| P2 AI와 정산 | 실제 이미지 어댑터, 작업 큐, 견적·예약·복원, 구독·충전·정기결제 | AC13부터 AC24 |
| P3 제작 범위 | 스탠드형, 구멍·바코드, 3D, 프로파일, 제작 게이트, 팀과 관리자 | AC07부터 AC12 및 AC25부터 AC29 |
| P4 유료 시험 | 제조사 실자료, 출력 검수, PG·AI 실연동, 운영 설정·백업 | 유료 시험 체크리스트와 AC30부터 AC32 |
| P5 확장 | 승인 박스 1종과 반복 상품 복제·변형 | 6면·접착부·접힘·3D와 재출력 검증 |

P1에서는 저장이 브라우저 메모리에만 남는 결과를 제출하지 않는다. P2에서는 모의 결제 성공과 PG 테스트 결제 성공을 별도 표시한다. P3에서는 demo 템플릿으로 제작 게이트가 열린 것을 성공으로 판단하지 않는다. P4 외부 조건이 부족하면 부족한 해당 검수만 열어 두고 완성된 개발·테스트 결과를 제출한다.

첫 실행 보고에는 발견한 기존 코드와 채택 스택, 만든 파일, 시작 명령, 현재 작동하는 세로 흐름, 다음 구현 항목을 짧게 적는다. 진행 중 질문은 실제로 선택이 필요한 외부 조건에 한정하고, 일반 UI 문구·파일명·구현 순서는 본 문서에 따라 진행한다. 사용자의 별도 지시가 있으면 본 문서의 제안보다 우선한다.

## 20 필수 검수 시나리오

검수는 실제 오류 위험을 확인한다. 단순히 화면 문구나 내부 함수 호출을 그대로 복제한 테스트로 통과 수를 늘리지 않는다. 날짜·결제·동시성은 통제 가능한 시험 시계와 모의 제공자로 재현하고 실제 API 시험은 별도로 기록한다.

| ID | 시험 | 기대 결과 |
|---|---|---|
| AC01 | 새 계정에서 프로젝트 생성·편집·저장·재열기·검토 출력 | 실제 DB와 파일로 전 과정 완료 |
| AC02 | Windows 한글 IME로 높은 단백질 함량 입력, 조합 중 Enter | 글자 누락·중복·조기 완료 없음 |
| AC03 | 한글·영문·숫자·줄바꿈·특수문자를 저장 후 PDF 비교 | 원문 보존, 누락 글리프 차단, 배치 일치 |
| AC04 | 두 탭 동시 저장과 네트워크 끊김 후 복구 | 버전 충돌 안내, 기존 데이터 무단 덮어쓰기 없음 |
| AC05 | 230×310mm 입력과 23×31cm 입력을 비교 | 같은 geometry와 PDF 크기, 계산 차이 0.01mm 이내 |
| AC06 | 단위 없는 치수·음수·범위 초과·불가능 실링 입력 | 안내 또는 서버 거절, 제작 출력 불가 |
| AC07 | 정상·오류 체크 숫자·선행 0과 바코드 축소 | 문자열 보존, 틀린 번호 차단, 디지털 판독 확인 |
| AC08 | 구멍을 텍스트·실링·바코드 여백에 겹침 | 해당 요소로 이동하는 blocker 표시 |
| AC09 | 스탠드형 앞·뒤·바닥에 방향 글자 표시 | 누락·반전 없이 2D·3D 일치 |
| AC10 | 박스 6면과 접착부·덮개 검사 | 도면과 조립 매핑 일치, P5 완료 조건 |
| AC11 | 검토 PDF와 제작 PDF를 같은 치수로 출력 | 1대1 크기, 검토 표시 구분, 원본 이미지·벡터 사용 |
| AC12 | 미승인·철회 템플릿, 낮은 PPI, 미지원 PDF/X 조건 | 제작용 서버 차단, 검토용 가능 |
| AC13 | API 키 없음·잘못된 키·제공자 한도·시간 초과 | 명확한 상태, fake 성공 없음, 무한 재시도 없음 |
| AC14 | 표준 3장·수정 1장·최초 출력 1건 | 총 80 차감과 각 내역 확인 |
| AC15 | 시안 3장 중 2장 성공 | 20 확정·10 반환, 실패 결과 미청구 |
| AC16 | 같은 키 재클릭·worker 재시작·큐 재전달 | 결과와 사용자 차감 각 한 번 |
| AC17 | 잔액 30에서 20짜리 요청 2개 동시 실행 | 하나만 예약, 잔액 음수 없음 |
| AC18 | 같은 항목의 문구·배치·수량 수정과 재출력 | 추가 차감 0, 새로운 검수와 리비전 |
| AC19 | 용량·바코드·완성 치수·구조 변경 | 새 항목 사전 안내와 최초 40 차감 |
| AC20 | 출력 일부 파일 누락·업로드 실패·DB 실패 | 묶음 미완료, 최초 권한·40 차감 없음 |
| AC21 | 월분 만료와 새 결제, 구매분 500 유지 | 월분 이월 없음, 결제당 지급 1회 |
| AC22 | 만료 직전 예약 후 실패·worker 소실 | 중복 만료 없음, 사용할 수 있는 복원분 |
| AC23 | 월말·윤년 갱신과 상향·하향 변경 | 올바른 주기·차액·정수 지급, 이중 결제 없음 |
| AC24 | 결제 응답 유실·중복 웹훅·실패 후 재조회 | 확인한 성공만 한 번 지급, 잘못된 주문 거절 |
| AC25 | A 조직 사용자가 B 조직 asset·job·파일 ID 요청 | 접근 차단과 정보 노출 없음 |
| AC26 | viewer의 편집·출력·차감·템플릿 승인 시도 | 서버 역할 검증으로 거절 |
| AC27 | 체험 생성·14일 만료·제작 출력 요청 | 30 범위 적용, 자동 결제·제작 출력 없음 |
| AC28 | 해지 후 구매분으로 작업, 다른 팀원 접근 | 소유자 사용 가능, 팀 접근 해제, 파일 보존 |
| AC29 | 악성 SVG·과대 파일·외부 자산 주소·HTML 문구 | 정화 또는 거절, 코드 실행·외부 접근 없음 |
| AC30 | 제조사 제출 10건에 통과·반려 기록 | 기술 원인·미적 수정 분리와 실제 비율 계산 |
| AC31 | DB·자산 백업 후 별도 환경 복구 | 장면·원장·파일 연결 복원, 결과 재열기 |
| AC32 | 운영 시작에 mock·demo·미확정 정책이 남음 | 유료 운영 게이트 닫힘과 부족 항목 안내 |

0.01mm는 좌표·단위 변환의 소프트웨어 오차 목표이며 제조 설비의 공차를 보장하는 수치가 아니다. 한글 PDF 검수는 텍스트 추출과 렌더링 비교를 함께 하며 윤곽선 글꼴 출력은 원문 manifest와 시각 비교로 확인한다. 실제 바코드 인쇄 검증은 디지털 디코딩과 별도 증빙으로 남긴다.

## 21 외부 준비 항목과 개발 기본값

| 필요한 항목 | 자료가 없을 때 구현 | 유료 시험 전 필요한 증거 |
|---|---|---|
| 제조사와 도면 | 자체 demo 봉투 2종, 승인 화면·검수 엔진 | 사용 허락, 도면·재질·규격과 승인 담당자 |
| PDF·색상·폰트 조건 | 기본 검토 PDF, capability와 게이트 | 제조사 프로파일과 실제 파일 입고 확인 |
| 바코드 | 데모 번호를 검토용으로만 사용 | 고객의 실제 번호와 인쇄 판독 |
| 이미지 제공자 | fixture와 실제 어댑터 코드 | 서버 API 키, 모델 접근, 원가·실행 확인 |
| 정기결제 | mock와 PG 테스트 어댑터 | 가맹·빌링 계약, 라이브 설정, 운영 정책 |
| 폰트·로고·제품 사진 | 배포 가능한 샘플과 빈 입력 | 실제 자산의 사용권과 폰트 지원 확인 |
| 서비스명·도메인 | Phoenix Packaging 개발 이름 | 사업자가 정한 운영 이름·도메인 |
| 배포 환경 | Docker Compose 로컬 실행 | 웹·API·worker·DB·저장소·백업 구성 |

개발 환경 기본값은 AI_PROVIDER=fixture, PAYMENT_PROVIDER=mock, STORAGE_DRIVER=local, ENABLE_LIVE_PAYMENTS=false, ENABLE_PRODUCTION_EXPORT=false다. production 환경에서는 fixture·mock가 켜진 유료 기능을 시작 검사로 차단한다. 키를 넣지 않았을 때 실제 결과처럼 보이는 성공 응답을 만들지 않는다.

운영 배포는 웹, API, worker, scheduler, DB, Redis, 저장소의 책임을 분리한다. 서버리스 웹 호스팅만으로 장시간 worker와 정기결제 스케줄러가 운영된다고 가정하지 않는다. 배포용 Dockerfile·헬스체크·마이그레이션·환경변수 예시·백업 및 롤백 절차를 제공한다. 도메인·유료 자원 생성과 공개 운영 전환은 완성된 결과를 검토할 수 있게 준비한 뒤 진행한다.

## 22 납품할 코드와 문서

Codex는 소스 코드, lockfile, DB 마이그레이션, 예시 환경변수, Docker Compose, seed, 라이선스 목록을 남긴다. API·편집·정산 핵심 검수의 실행 결과와 브라우저 화면, 실제로 열리는 샘플 검토 PDF를 제공한다. 제조사 승인 파일이 있으면 해당 조건의 제작 묶음도 포함한다.

README에는 최초 실행·중단·재시작, 계정 생성, 테스트 데이터 초기화, 실제 제공자 연결, 백업·복구와 문제 해결을 적는다. docs/acceptance-results.md에는 AC ID별 통과·실패·미실시와 증빙 위치를 기록한다. 미실시는 통과로 집계하지 않는다. docs/production-readiness.md에는 외부 준비 항목과 승인 조합을 적는다.

설정 샘플과 scene 예시는 개발 계약을 설명하는 보조자료다. 예시 schema가 모든 포장 구조와 출력 요건을 완전히 정의한다고 가정하지 말고 본 명세에 맞춰 운영용 스키마와 semantic validation을 완성한다. 보조자료보다 본 문서의 업무 규칙을 우선하고, 충돌이 발견되면 결정 기록에 남겨 일치시킨다.

최종 인수 조건은 실제 프로젝트 저장과 재편집, 입력 한글 보존, 정확한 치수의 검토 파일, 승인된 조건의 제작 출력, 중복 없는 과금과 실패 복구다. 보기 좋은 화면만으로 인수하지 않는다. 외부 제조사·결제 조건이 충족되지 않은 단계에서는 그 조건을 제외한 개발 완성도와 시험 결과를 명확히 제출한다.

## 23 참고 근거

S0. 피닉스AI 「AI 패키지 제작 자동화와 ERP 품절 처리 사업 전략」 최종판. 2026년 9월 16일 수정본. 포장디자인 요구, 고객층, 요금·크레딧 제안과 유료 검증 기준의 근거. 이 작업지시서의 API·DB·화면·동시성·반올림·기술 기본값은 이를 구현하기 위해 구체화한 개발 설계다.

S1. OpenAI Image generation. 이미지 생성·편집 API, 모델별 설정. 2026년 9월 16일 확인. https://developers.openai.com/api/docs/guides/image-generation

S2. Konva Text editing in HTML5 canvas. HTML 입력 요소를 이용한 캔버스 텍스트 편집. 2026년 9월 16일 확인. https://konvajs.org/docs/sandbox/Editable_Text.html

S3. ePac Flexible Packaging Pre Press Information. 제조사별 색상·폰트·블리드·레이어·화이트 인쇄 조건의 사례. 2026년 9월 16일 확인. https://epacflexibles.com/pre-press-information/

S4. GS1 Korea 유통표준코드 FAQ와 바코드 검증 서비스. 번호 부여와 인쇄 바코드 검증의 구분. 2026년 9월 16일 확인. https://www.gs1kr.org/front/board/appl/FAQ1.asp 및 https://www.gs1kr.org/front/board/appl/BVService.asp

S5. Toss Payments 자동결제 결제창 연동. 상점 측 일정 실행과 서버 승인 처리. 2026년 9월 16일 확인. https://docs.tosspayments.com/guides/v2/billing/integration

S6. Toss Payments 웹훅 이벤트 및 자동결제 개요. 자동결제 성공 웹훅에 의존하지 않는 정산과 계약 조건. 2026년 9월 16일 확인. https://docs.tosspayments.com/reference/using-api/webhook-events 및 https://docs.tosspayments.com/guides/v2/billing
